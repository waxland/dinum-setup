---
title: "Implémentation du Provider Django /marche"
sidebar_label: "01. Provider Django"
description: Code source et structure de ProcurementSourceProvider dans django-lasuite-sources.
---


---

## 🐍 1. Implémentation `ProcurementSourceProvider`

```python
from typing import List, Optional
import httpx
from lasuite_sources.base import BaseSourceProvider
from lasuite_sources.types import SourceSearchResult, SourceSuggestResult

class ProcurementSourceProvider(BaseSourceProvider):
    name = "BOAMP / Marchés Publics"
    source_type = "procurement"
    aliases = ["marche", "boamp", "achat", "dce", "dae"]
    description = "Recherche d'avis de marchés publics et AAPC de l'État et des collectivités"

    async def search(self, query: str, limit: int = 10) -> List[SourceSearchResult]:
        # Appel API BOAMP avec validation anti-SSRF et cache SHA-256
        ...

    async def suggest(self, query: str, limit: int = 5) -> List[SourceSuggestResult]:
        # Autocomplétion temps réel
        ...
```
